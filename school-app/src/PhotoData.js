// // data.js

// // Import all images from the '2021 photos' folder dynamically
// const importAll = (r) => r.keys().map(r);
// const images = importAll(
//   require.context("../assets/christmas-2006", false, /\.(png|jpe?g|svg)$/)
// );

// // Export the array of imported image paths
// export default images;
